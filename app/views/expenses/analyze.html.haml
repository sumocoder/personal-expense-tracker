
%h1 Expense Analysis

%div{id: "expenses_chart", style: "width:100%; height:400px;"}

.row.expense_analysis_summary
  .col-lg-6.col-md-offset-3
    %table.table.table-striped
      %thead
        %tr
          %th Category
          %th Total
          %th %
      %tbody
      - @expenses_grouped.each do |expense_group|
        %tr
          %td= expense_group.name
          %th= expense_group.total_cost
          %td= (expense_group.total_cost / @categories_total)*100
%script{src: "http://code.highcharts.com/highcharts.js"}
